# 本仓库不把真实密钥提交进 Git。按环境复制下面的示例文件：
#
# Go 服务 Docker env（推荐）
#   cp .env.dockersentiment.example .env.dockersentiment
#
# 前端 Docker
#   cp ruoyi-fastapi-frontend/.env.docker.example ruoyi-fastapi-frontend/.env.docker
#
# 前端本地开发
#   cp ruoyi-fastapi-frontend/.env.development.example ruoyi-fastapi-frontend/.env.development
#
# 必改项：JWT_SECRET_KEY、MySQL 密码、默认 admin 密码。
# 16 GiB 云主机：SFP_DATA_ROOT=/workspace/sfp-data（slim overlay 默认 bind，见 docs/SFP-TWO-HOST-DEPLOY.md）
# 可选：长桥 APP_KEY/SECRET/TOKEN、AI Base URL / API Key。
# 生产路径为 Go-only。Python RuoYi 后端已从 main 删除，见 docs/PYTHON-REMOVED.md。
# 完整步骤见 docs/DEPLOY.md 与 README.md。
