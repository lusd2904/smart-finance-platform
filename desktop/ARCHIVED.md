# Electron 桌面端已归档

2026-08 起桌面正式端是 `flutter_client/`（登录后 WebView 打开网关 Web 控制台）。

本目录仅保留 git 历史，**不再构建、不再发版、不再修功能**。根目录 `npm run desktop` 已移除。

新环境请用：

```bash
cd flutter_client
flutter run -d macos   # 或 windows
```
