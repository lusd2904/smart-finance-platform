# 四端客户端统一规划（macOS / Windows / iOS / Android）

> 2026-08-24 立项。本文档是四端客户端的唯一规划基线，后续里程碑变更在此文档上迭代。

## 目标

用 **Flutter 单工程**交付四个原生客户端（iOS、Android、macOS、Windows），功能与 Web 管理台**全量对齐**（行情、舆情、量化、交易、AI 研判），四端同步首发。替代现有 `desktop/` Electron 壳；`ruoyi-fastapi-app/`（uni-app）的 App 侧职责由本工程接管。

## 已确认决策（2026-08-24 Q&A）

| 决策点 | 结论 | 备注 |
|---|---|---|
| 技术路线 | Flutter 四端一体，单工程出四包 | 官方正式支持四端的唯一方案 |
| 功能范围 | 四端全量对齐（含量化研究 + 长桥交易） | 手机端复杂交易界面需单独交互设计 |
| 桌面端 | 推倒 Electron 重做 | `desktop/` 过渡期继续服务，Flutter 桌面对齐后下线 |
| 交付节奏 | 四端同步首发 | 每个里程碑内四平台同跑同验 |
| 分发基线 | Android APK 直装 + iOS TestFlight + macOS 公证 dmg + Windows 无签名 exe | iOS 只走 TF 不上架商店；Windows 接受 SmartScreen 警告不买证书；商店上架列为后续合规任务线 |

## 现状调研（规划依据）

- **后端**：FastAPI 统一 JWT 鉴权；网关 = 前端 nginx 反代 `/docker-api`（`docker-compose.sentiment.yml`，端口 12580）；四端可零改动复用同一套接口。
- **实时通道**：后端**无 WebSocket/SSE**，行情台报价读 Redis / SWR + 轮询；交易实时在独立进程。客户端第一期同样走轮询即可。
- **桌面现状**：`desktop/`（Electron 34）：首启「网关配置 → 探测通过 → 登录页」交互成熟，含沙箱桥接白名单、网关地址校验提示。这套交互在 Flutter 中复刻。electron-builder 已配 mac dmg/zip + win nsis，但无签名公证、无自动更新。
- **移动现状**：`ruoyi-fastapi-app/`（uni-app 3.0）只有登录/注册/工作台/我的骨架页，无金融业务页面；默认构建目标是微信小程序。`todos.md` 第 14 条「移动端暂不升级」的约定被本规划取代（App 侧）；其 H5 有活跃用户，**双轨保留**（不归档），Flutter 接管原生 App 职责。
- **接口域清单**（客户端按此对齐）：
  - `system`：登录/注册/验证码/用户信息/菜单权限
  - `market`：市场热度、Top50 快照、全部股票分页（`/market/instrument/universe`）、自选（`market_watchlist` 按 user_id）、日/周/月 K线、分时（Influx `minute_kline`）、财经资讯、AI 研判结果
  - `sentiment`：舆情大盘、资讯列表、分析历史
  - `quant`：因子、IC/IR/分层收益、策略信号、扫描台账、8 族权重、长桥配置（按用户行，保存掩码）
  - `trade`：长桥账户/持仓/委托、盘口深度、纸面自动交易、风控规则与事件、通知中心
- **CI**：`.github/workflows/ci.yml` 已有 pip-audit 门禁与双镜像构建冒烟；Flutter 构建流水线为新增矩阵。

## 目标架构

### 工程位置与命名

```
flutter_client/                # 新增，命名跟随仓库平铺惯例（backend/frontend/app/desktop）
├─ lib/
│  ├─ main.dart
│  ├─ core/                    # 基础设施
│  │  ├─ gateway/              # 网关配置+探测（复刻 desktop 交互）
│  │  ├─ api/                  # Dio 客户端、JWT 拦截器、刷新、错误码映射
│  │  ├─ storage/              # flutter_secure_storage（token）/ shared_preferences（偏好）
│  │  ├─ router/               # go_router，桌面宽屏/手机窄屏双布局壳
│  │  └─ theme/
│  ├─ features/
│  │  ├─ auth/                 # 登录/注册/验证码
│  │  ├─ market/               # 热度、Top50、全部股票、行情台
│  │  ├─ watchlist/            # 自选三栏（分组沿用 note 逗号解析）
│  │  ├─ kline/                # 日/周/月 K线 + 分时（CustomPainter 自绘）
│  │  ├─ news/                 # 财经资讯流、舆情只读页
│  │  ├─ ai/                   # AI 研判结果查看
│  │  ├─ quant/                # 因子、IC/IR/分层收益图、信号、扫描台账
│  │  ├─ trade/                # 长桥账户、持仓委托、盘口深度、纸面交易、风控
│  │  └── notice/              # 通知中心
│  └─ shared/                  # 通用组件（自适应表格、报价单元、涨跌色）
├─ android/ ios/ macos/ windows/   # 四个平台壳
└─ integration_test/           # 关键路径 e2e
```

### 关键技术选型

| 关注点 | 选择 | 理由/备选 |
|---|---|---|
| 状态管理 | Riverpod | 社区主流、可测；备选 bloc |
| 网络 | Dio + JWT 拦截器 | 对齐后端 `code/msg/data` 响应包与 token 失效语义 |
| K线图表 | CustomPainter 自绘蜡烛图/分时 | 金融 K线无完美现成包；起步可用 `fl_chart` 占位再替换 |
| 数据表格 | Syncfusion DataGrid | 注意社区许可边界（公司收入 <$100 万免费）；备选 `material` DataTable2 |
| 自适应布局 | 单代码库双壳：手机底部导航单栏；桌面侧栏 + 多栏 | 自选三栏等页面按断点切换 |
| 平台差异 | 条件导入 + `Platform` 判断 | 推送、窗口行为、更新检查各自实现 |

### 后端配合改造（新增任务线）

1. **版本检查接口** `GET /app/version?platform=&version=`：返回最新版本、下载地址、强制升级标志（APK 直装渠道必需）。
2. **推送服务**（module_push）：APNs + FCM；事件源对接风控事件、成交回报、涨跌提醒（jobs 队列投递）。第一期降级方案：应用内通知轮询，推送作为 M5 增强。
3. **（可选优化）SSE/WebSocket 行情通道**：替代轮询；非首发阻塞项。
4. **中间件兼容核查**：确认 IP 归属地、审计日志、限流对非浏览器 UA / 无 Referer 请求的兼容。

## 里程碑（纵切功能，每步四平台同跑同验）

规模标记：S ≈ 数天级，M ≈ 1~2 周，L ≈ 2~4 周（单人粗估，仅供排期感知，非承诺）。

### M0 · 工程地基（M）
- `flutter create` 四平台脚手架、目录结构、主题（涨跌红绿、暗色）
- Dio + JWT 拦截器 + 错误码统一处理
- 网关配置页：首启必配 → 探测健康接口 → 通过才进登录（复刻 desktop 语义，含错误地址提示）
- 登录/注册/验证码；token 安全存储
- CI：GitHub Actions 四平台构建矩阵（ubuntu→android apk、macos→ios+macos、windows→windows exe），产物上传 artifact
- **验收**：四平台各出一个可安装包，能配网关、登录、看到空壳首页

### M1 · 行情域（L）
- 市场热度看板（三市场指数/成交额/涨跌家数）、Top50
- 全部股票分页浏览 + 搜索（对齐 `/market/instrument/universe` 强制分页）
- 自选三栏：分组（note 解析）、列表、加删、概览
- K线：日/周/月 + 分时；标的详情页
- **验收**：手机上加自选→看K线闭环流畅；桌面三栏布局完整呈现

### M2 · 资讯 + AI + 通知（M）
- 财经资讯流（分页、下拉刷新）、舆情大盘只读版
- AI 研判结果查看（单标的 + 批量扫描结果）
- 通知中心（应用内轮询）
- **验收**：与 Web 端同一账号数据一致

### M3 · 量化域（L）
- 因子列表、IC/IR 图、分层收益图（图表组件复用 M1 绘制能力）
- 策略信号、扫描台账、8 族权重展示
- **验收**：量化核心只读链路四端可用

### M4 · 交易域（L，风险最高）
- 长桥账户绑定（按用户行，掩码回显）、连通性测试
- 持仓/委托列表、盘口深度 + 分时
- 纸面自动交易开关与状态（默认不实盘下单的语义原样保留）
- 风控规则与事件
- 移动端下单交互单独设计（金额步进器、二次确认、防误触）
- **验收**：纸面模式下完整下单-撤单-成交流程；实盘开关保持默认关闭

### M5 · 发布工程（M~L）
- macOS：签名 + 公证（Gatekeeper 放行）；dmg 产物
- Windows：代码签名证书签名（可选但推荐）；NSIS/MSIX 安装包
- Android：release 签名 APK + 官网直装页（含版本检查接口对接）
- iOS：TestFlight 内测分发
- 自动更新：桌面端启动检查新版本 → 引导下载；移动端走版本检查接口强更提示
- **验收**：全新设备从官网/链接安装到使用的完整走通；旧版本收到升级提示

### 收尾 · 旧端处置
- Flutter 桌面端功能对齐并灰度稳定后，`desktop/` Electron 归档（保留 git 历史，README 标注迁移说明）
- `ruoyi-fastapi-app/` 双轨保留：H5 活跃用户继续服务，Flutter 接管原生 App；小程序维持现状。

## 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| Dart/Vue 团队技能差 | 前期速度慢 | 声明式 UI 思维相通；M0 设为练兵场；关键模式写 ADR |
| K线自绘工作量超预期 | M1 延期 | 先 fl_chart 占位保闭环，自绘作为独立增强任务并行 |
| Syncfusion 许可边界 | 法务风险 | M3 前评估收入规模，不满足即切开源 DataTable2 |
| 四端同步首发互相阻塞 | 全体延期 | 「首发」以官网 APK/dmg/exe 直装为准，不依赖商店审核；TF/APK 通道先行内测暴露问题 |
| ~~Apple Developer 账号未落实~~ | 已关闭 | 账号已具备（2026-08-24 确认），iOS TF 与 macOS 公证无阻塞 |
| 移动端长桥下单合规敏感性 | 审查风险 | 纸面模式默认；实盘入口移动端第一期隐藏与否留到 M4 前决策 |

## 测试策略

- **integration_test**：每里程碑覆盖关键路径——登录→热度→加自选→开K线（M1）；研判查看（M2）；因子图表加载（M3）；纸面下单闭环（M4）；版本检查提示（M5）。
- **场景对照**：移植 `scripts/web_e2e.mjs` 场景清单作为四端验收基线。
- **CI**：四平台构建矩阵绿 + `flutter test` 单测 + integration_test 在 android/macos 两代表平台跑通（windows/ios 构建冒烟即可）。

## 决策关闭记录（2026-08-24）

1. ~~Apple Developer 账号~~ → **已具备**。iOS 走 TestFlight，不正式上架 App Store。
2. ~~uni-app H5 去留~~ → **双轨保留**，H5 有活跃用户不归档；Flutter 只接管原生 App。
3. 行情实时性：维持轮询语义起步，SSE/WebSocket 作为后端可选优化线（唯一保留的开放项）。
4. ~~Windows 签名证书~~ → **不购买**，接受 SmartScreen 警告（可右键继续运行）。
