# SFP 手机端（逻辑宽 390px）逐页布局设计规范

本规范专为 **SFP (Smart Finance Platform) 移动端（iOS / Android）** 在逻辑宽度约 **390px**（如 iPhone 13/14/15 标配尺寸）下的单手竖屏布局提供重构设计。

所有页面严格遵循 **墨蓝金融终端**（Cyber / Slate Navy）风格，确保深色一等公民（ scollfoldBackgroundColor: `#10141C` ）与高密度的金融数据显示。

---

## ⚠️ 通用适配与设计规范

1. **4 Tab 底部导航架构**：
   * 采用半透明模糊的 **CupertinoTabBar**，常驻底栏。
   * **4 个核心 Tab** 为：**【工作台】**、**【行情】**、**【自选】**、**【我的】**。
   * **非根页面（如 K线、交易终端等）**：顶栏显示「返回」按钮，且底栏 TabBar 依然保留，支持标准横向滑动推入（Push Navigation）及屏幕边缘左滑返回（Interactive Pop Gesture）。
2. **弹窗与模态规范**：
   * 移动端禁止使用桌面端的大号居中 Dialog，全面重构为符合单手操作习惯的 **自适应可拖拽底部抽屉（Draggable BottomSheet）**，顶部带有 `36px × 4px` 灰色药丸指示条（Grabber）。
3. **视觉与触感反馈**：
   * **数字排版**：所有价格、涨跌、成交额统一开启 `tabular-figures`（等宽数字），解决数值跳动抖动。
   * **颜色令牌**：品牌蓝 `#409EFF`、涨红 `#E5484D`、跌绿 `#30A46C`、平灰 `#8A9099`、警示黄 `#F5A524`。
   * **振动触感**：下单 `medium`、撤单 `light`、风控告警 `heavy`、分组或 Segment 切换 `selectionClick`。

---

## 逐页布局设计明细

### 1. 门户 (Portal)

* **所属 Tab 归属**：**【工作台】**（主页路由 `/portal`）
* **从上到下模块**：
  1. **头部问候区**：简洁的「早安，交易员」，右侧为用户头像与消息通知铃铛（带红色未读角标）。
  2. **资产卡片（翻页轮播 Carousel）**：
     * *Slide 1 (资产视角)*：展示账户总资产、现金余额、持仓总市值、当日盈亏（金额+百分比），支持「一键睁眼/闭眼」隐藏隐私金额。
     * *Slide 2 (指数视角)*：横向滚动的三个主要市场大盘指数小滑块（美/港/A）。
  3. **九宫格快捷金刚区**：4列自适应圆角图标宫格，图标带有浅蓝色半透明圆形底座（字号 `11sp`）。包含：*行情看板、全部股票、智能选股、自选清单、舆情大盘、量化研究、交易台、AI研判、通知中心*。
  4. **AI 动态与快讯卡片**：通宽流式卡片瀑布流，标题 `22sp`，内容链接自动折行。
* **主手势**：
  * **左右横滑**：在资产卡片区左右横向滑动，切换资产数据与市场指数速览。
  * **下拉刷新 (PTR)**：单手在顶部下拉，触发全局数据与资产状态刷新。
  * **点击操作**：点击九宫格功能卡片进入相应模块；点击 AI 动态卡片内链接跳转至子页。
* **必留字段**：
  * 用户头像、昵称、会话健康状态灯（StatusDot，如绿色表示行情网关良好）。
  * 账户总资产、当日浮动盈亏（含正负号变色）。
  * 快捷入口名称与对应图标。
  * 最新 AI 动态快讯标题、一句话核心观点与发布时间。
* **可收进「更多」的字段**：
  * 门户原本超长的桌面端大标题「量化交易与 AI 研判综合指挥中心」在 390 宽手机端直接不予展示。
  * 系统后台监控参数（如 APScheduler 任务状态、Redis缓存、数据库连接池、Grok队列积压数等）全部收拢进顶部左侧汉堡菜单（RuoYi 全量菜单）中。
  * 具体的网关连接地址与探测延迟详情，收拢到网关配置抽屉中。

---

### 2. 行情热度 (Market Heat)

* **所属 Tab 归属**：**【行情】**（行情首页路由 `/market/heat`）
* **从上到下模块**：
  1. **顶部固定市场切换胶囊**：通栏 Segmented 市场切换按钮（美股 US / 港股 HK / A股 CN）。
  2. **中上部大盘指数滑块条**：美港A主要指数横向滑动展示条。
  3. **中部市场概貌（极简进度条）**：红绿双色占比温度条（精炼展示：上涨 2150 | 45% vs 下跌 2400 | 50%）。
  4. **下方 Top50 龙虎榜双行列表**：每行采用双行式紧凑金融列表项，行高可点。
* **主手势**：
  * **点击操作**：点击美/港/A胶囊触发市场切换（触感 `selectionClick`）；点击 Top50 列表行，无缝 Push 跳转进入该股 K 线详情页。
  * **横向滑动**：左右滑动指数滑块条速览；**整行右滑** 快速触发「加自选 / 删自选」快捷操作。
  * **下拉刷新 (PTR)**：页面下拉，重新获取 Top50 排行数据与市场涨跌分布。
* **必留字段**：
  * 三个市场切换 Tab 字符（US / HK / CN）。
  * 主要指数名称、当前最新点位、涨跌额、涨跌幅。
  * 市场涨跌家数比、红绿分布进度条。
  * 龙虎榜字段：排名、标的代码、标的名称、最新成交价、涨跌幅徽章（红/绿实色底色，如 `+5.82%`）。
* **可收进「更多」的字段**：
  * 龙虎榜次要属性（如成交额、换手率在双行中合并展示在代码下方），其他类似量比、振幅、主力资金净流入等细节字段全部收起，不占用首屏列表宽度。
  * 电脑端并排展示的「全市场成交额分布多段柱状图」在手机端剔除，不予直观展现。

---

### 3. 自选 (Watchlist)

* **所属 Tab 归属**：**【自选】**（自选列表路由 `/market/watchlist`）
* **从上到下模块**：
  1. **顶部分组 Filter Chips 横滑区**：自定义分组小芯片（如 `[ 全部 18 ]` `[ 核心科技 6 ]` `[ 港股自选 4 ]` `[ + 新建 ]` ）。
  2. **自选股概览微卡**：精简展示自选股总数、今日上涨/下跌家数、平均涨幅。
  3. **主体自选股列表**：单列轻量虚拟列表，每行高度紧凑。
* **主手势**：
  * **横向滚动**：顶部的自选分组 Chips 支持横向滑动。
  * **左滑操作**：列表行向左滑动，露出「置顶」与「删除」红色图标按钮。
  * **长按拖拽**：在自选列表中长按单行，触发拖拽，支持上下自定义排序。
  * **点击操作**：点击分组芯片快速过滤标的；点击自选列表行，直接 Push 进入标的 K 线详情页。
  * **下拉刷新 (PTR)**：下拉刷新自选行情。
* **必留字段**：
  * 自选分组名称（含标的个数）。
  * 标的代码、标的中文名称。
  * 实时最新价、涨跌幅百分比（右侧带圆角红/绿实色涨跌幅徽章，字体加粗）。
* **可收进「更多」的字段**：
  * 自选股小時级 AI 综合研判（技术面+舆情前瞻）全部收进 K 线详情页展示。
  * 自选添加时间、关联行业板块、添加时的价格（成本）收拢至单标的属性抽屉或编辑面板。

---

### 4. 标的详情与多周期 K 线 (Symbol Detail & Advanced Kline)

* **所属 Tab 归属**：作为 **【行情】/【自选】Tab 的二级非根页面**（非根页，顶部返回，底栏仍在，支持边缘左滑返回）。
* **从上到下模块**：
  1. **头部极简报价板**：左侧展示大字报价（最新价、涨跌额、涨跌幅），中部并排展示今开、最高、最低、成交量 4 个最核心交易指标。右侧放置「加自选/已自选 (★)」与「全屏横屏 (⛶)」图标。
  2. **中部周期切换条**：SegmentedButton 周期选择条（分时 / 日K / 周K / 月K / 更多）。
  3. **中部自绘 K 线区域**：全宽绘制区（支持主图技术指标 MA/BOLL，副图技术指标 VOL/MACD）。
  4. **中下部分段/折叠辅助面板**：标签页切换器，用于切换查看【盘口五档】/【逐笔明细】/【AI 研判】/【资讯】。
  5. **底部固定悬浮交易栏**：常驻两个全宽色块大按钮：【买入】（红色 `#E5484D`）与【卖出】（绿色 `#30A46C`）。
* **主手势**：
  * **双指捏合/张开 (Pinch)**：缩放 K 线时间跨度，改变首屏显示的 K 线根数。
  * **单指横向拖拽**：滚动查看历史 K 线。
  * **长按滑动**：在 K 线图上长按 0.2s 激活垂直/水平十字交叉光标（Crosshair），滑动手指吸附查询对应的 OHLCV 及技术指标明细。
  * **点击操作**：点击买入/卖出，直接从底部拉起「快捷交易下单抽屉」。
* **必留字段**：
  * 股票代码、最新价、涨跌额、涨跌幅（头部大字）。
  * 头部 4 个基础指标（今开、最高、最低、成交量）。
  * 当前十字光标对应的 K 线开盘、收盘、最高、最低价、成交量。
  * 五档盘口的价格、挂单量背景占比条。
* **可收进「更多」的字段**：
  * 其余繁杂的盘口指标（如振幅、换手率、PE/PB、成交额、总市值等）收拢进头部价格右侧的「下拉展开/更多指标」抽屉中。
  * 电脑端并排展示的「十档盘口」与「逐笔明细」降级为手机端分段 Tab，非首屏平铺。

---

### 5. 交易终端 (Trading Terminal)

* **所属 Tab 归属**：属于 **【行情】/【自选】/【工作台】Tab 的二级非根页面**。一般由 K线页的【买入】/【卖出】按钮或工作台九宫格直接触发，以 **Draggable BottomSheet（底部交互式下单抽屉）** 呈现。
* **从上到下模块**：
  1. **头部标的信息区**：当前交易标的代码/名称切换器（点击呼出搜索框）+ 实时最新价/买一卖一跳动大字。
  2. **中部核心交易区（55:45 左右分栏）**：
     * *左侧（55% 宽）*：高度压缩的买卖五档盘口（带挂单量背景占比条）。
     * *右侧（45% 宽）*：买入/卖出方向分段开关（红色/绿色实色高亮），订单类型选择器（限价 Limit / 市价 Market / 止盈止损 Stop）。
  3. **中下部价格与数量配置区**：
     * 价格步进器：带有大号「+」和「-」的调整按钮（支持长按连续步进）。
     * 数量输入框 + 4个快捷仓位比例胶囊（1/4, 1/2, 3/4, 全仓）。
     * 资金动态显示行：可用资金、最大可买/可卖股数、预估成交金额。
  4. **底部固定提交栏**：
     * 全宽 48dp 高的大号实色提交按钮：【纸面买入】（红色）或【纸面卖出】（绿色）。
* **主手势**：
  * **长按操作**：长按价格步进器的「+」/「-」实现价格的连续快速步进。
  * **点击操作**：点击左侧五档盘口中任意档位的价格，该价格自动填充进价格输入框。
  * **右滑确认 (Slide to Confirm)**：点击提交按钮后，从底部滑出 **二次防呆确认弹窗（BottomSheet Confirmation）**，用户必须进行 **右滑拖拽滑动条** 完成下单，彻底杜绝误触。
* **必留字段**：
  * 标的代码与名称、买卖方向（Buy/Sell）、订单类型。
  * 委托价格、委托股数、仓位比例。
  * 可用资金、最大可买/可卖、预估订单总额。
  * 盘口五档最新价与挂单数。
* **可收进「更多」的字段**：
  * 复杂的风控参数设置（如单标的持仓上限、单日最大亏损额等）以及自动 Rebalance 调仓任务等，统一收拢进「策略设置」中。
  * 下单协议、费率计算公式等细节收纳进信息提示框。

---

### 6. 持仓与委托 (Positions & Orders)

* **所属 Tab 归属**：属于 **【工作台】/【行情】/【自选】的二级非根页面**。在交易中心可通过顶部的 Segmented 栏（【交易】/【持仓】/【委托】）无缝滑动切换。
* **从上到下模块**：
  1. **顶部切换与过滤栏**：Tab 分段控制器（【持仓】/【委托】）+ 顶栏右侧「筛选」漏斗图标（点击拉起筛选抽屉）。
  2. **主体单列卡片列表区**：
     * **【持仓列表卡片】**：
       * 顶部：证券代码 + 标的中文名称。
       * 中间：可用数量 / 持仓数量、持仓市值，持仓成本价 vs 最新现价。
       * 底部：当日盈亏、持仓累计浮动盈亏（金额 + 百分比，带红涨跌绿令牌色，等宽数字）。
       * 右下角：红色「平仓」快捷小按钮。
     * **【委托列表卡片】**：
       * 顶部：证券代码 + 名称 + 买入/卖出方向标签。
       * 中间：委托价格 vs 最新现价、委托股数 vs 已成交数量、委托时间。
       * 底部：订单状态（未成交、部分成交、已撤单）。
       * 右下角：常驻灰色「撤单」按钮（仅可撤单状态展示）。
* **主手势**：
  * **点击操作**：点击「筛选」按钮拉起右侧/底部筛选抽屉；点击持仓卡片跳转至该股 K 线图。
  * **下拉刷新 (PTR)**：整个页面下拉，触发数据刷新，重新获取持仓与委托明细。
  * **振动反馈**：点击「撤单」触发 `light` 触控反馈。
* **必留字段**：
  * 持仓：证券代码、名称、可用数量、持仓市值、最新价、浮动盈亏额与盈亏比（如 `+2.15%`）。
  * 委托：证券代码、名称、买卖方向、当前状态（未成交/部分成交/已撤单）、委托价格与数量、成交数量。
* **可收进「更多」的字段**：
  * 复杂的订单筛选检索面板（如时间范围、特定股票类型、特定交易通道等）一律收进顶栏「筛选」抽屉中。
  * 历史委托记录（非今日订单）统一放入底部的「历史明细」二级页面中。
  * 单卡片中的券商子账号、清算汇率等静态非高频参数收拢进卡片点击后的抽屉面板中。

---

### 7. 舆情大盘 (Sentiment Dashboard)

* **所属 Tab 归属**：属于 **【工作台】** 相关的非根关联页（可通过工作台九宫格快捷入口或顶部汉堡菜单打开）。
* **从上到下模块**：
  1. **顶部情绪仪表盘**：极简环形进度仪表盘（展示今日市场情绪得分，如 `78分 · 偏向乐观`），下方带有三市场情绪小药丸标签。
  2. **中部热门舆情主题区**：热门舆情主题词/关键字水平滚动标签组。
  3. **下部资讯无限信息流**：类社交媒体 Timeline 的实时资讯单列卡片信息流。
     * 顶部：`[ 资讯来源 · 时间戳 ]` `[ 标的名称/代码 · AI情绪标签 · 影响度星级 ]`
     * 中部：新闻标题（加粗 `15sp`）
     * 底部：AI 一句话核心观点（浅灰色背景引用框）
* **主手势**：
  * **下拉刷新 (PTR)**：单手在屏幕下拉，触发全网舆情即时采集与重新计算，刷新信息流。
  * **点击操作**：点击任意新闻标题卡片，向上拉出底部抽屉（PageSheet），以沉浸式视图展现完整原文及 AI 影响度分析。
* **必留字段**：
  * 情绪大盘今日分值与定性评级，以及三市场小药丸标签。
  * 新闻标题、来源、发布时间。
  * AI 情绪判定标签（利多/利空/中性）、情绪影响度星级（1-5星）。
  * AI 一句话核心观点（精炼高亮）。
* **可收进「更多」的字段**：
  * 定时任务配置、Cron表达式、提示词（Prompt）模板编辑等系统后台参数，完全收归至管理员后台，前台手机端不予平铺。
  * 大盘 30 天历史情绪走势折线图默认折叠，可收进情绪分数的点击展开详情中。
  * 历史大盘影响研判报告归档，收拢至二级历史记录页。
