# Emergency overlay: restore Python APP_MODULE=platform container (ws/jobs, open/sync were on Go sfp-backend).
#
#   docker compose -f docker-compose.sentiment.yml \
#     -f docker-compose.sentiment.slim.yml \
#     -f docker-compose.sentiment.platform-python-fallback.yml \
#     --profile python-platform-fallback up -d sentiment-backend
#
# Also revert nginx ws/jobs + open/sync to sentiment-backend in nginx.dockersentiment.slim.conf.

services:
  sentiment-backend:
    profiles: [python-platform-fallback]
