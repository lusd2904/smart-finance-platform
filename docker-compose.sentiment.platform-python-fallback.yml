# Emergency overlay: restore Python APP_MODULE=platform (not default slim).
#
#   docker compose -f docker-compose.sentiment.yml \
#     -f docker-compose.sentiment.slim.yml \
#     -f docker-compose.sentiment.platform-python-fallback.yml \
#     --profile legacy-python up -d sentiment-backend
#
# Also revert nginx ws/jobs + open/sync to sentiment-backend (do not ship that in slim conf).

services:
  sentiment-backend:
    profiles: [legacy-python]
